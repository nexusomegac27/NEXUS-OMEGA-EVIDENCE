# R5 tooling

R5 reference implementations, validators, migration tools, and reproduction entry points belong here. They should trace to `docs/r5/`, `schema/r5/`, `tests/r5/`, and `validation/r5/` artifacts.
