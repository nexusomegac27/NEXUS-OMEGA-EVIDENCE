# NEXUS Fundus C1

Reference index of adjudicated checkpoints. Not a truth authority. HOLD and negative knowledge are first-class and must not be dropped.

## Immutable head snapshots

Current mutable discovery head: `NEXUS_FUNDUS_HEAD.json`.

Historical head snapshots are retained under `heads/`. Git history plus explicit snapshots preserve predecessor states; updating the discovery head does not rewrite the earlier scientific state.

Current IX R1 snapshot: `heads/NEXUS_FUNDUS_HEAD_20260920_R1.json`.

```text
FUNDUS_REFERENCE != TRUTH_AUTHORITY
HOLD_AND_NEGATIVE_KNOWLEDGE = FIRST_CLASS
IX_EXECUTION = NOT_STARTED
```
