# IX R3 → R4 canonical carry-forward

R3 is operator-closed for handoff. The following gaps remain first-class and must be inherited by R4.

## G1 — R3-C three-state semantics

Execute the previously defined comparison between an explicit NEXUS epistemic state model and
Strong Kleene K3. Require explicit truth tables and conformance tests.

```text
NUMERIC_BALANCED_TERNARY != EPISTEMIC_K3
```

## G2 — R3-D provenance portability

Map one real NEXUS source → method → result → correction chain into W3C-PROV-compatible objects.
Round-trip it and test preservation of hashes, negative knowledge, corrections and supersession.

## G3 — R3-E substrate swap

Run one bounded task across at least two genuinely different model/runtime combinations under the
same authority boundary. Measure boundary preservation, recovery, evidence delta and canonical-state
continuity. Same answer alone is not continuity.

## G4 — canonical object identity collision

Resolve the duplicate logical object ID
`NEXUS_OMEGA_R3AB_CROSSVALIDATION_ORDER_TO_MISTRAL_VIBE_20260920_R0` without deleting either byte
variant. Assign distinct derivation IDs and preserve both hashes.

## G5 — executable ZKP comparison only if privacy need is explicit

Do not benchmark ZKP generically. First declare one privacy/selective-disclosure predicate. Then run
an actual circuit/proof/verifier and compare it with the signed-receipt baseline. If no concrete
privacy requirement exists, keep ZKP unimplemented.

## G6 — production boundary engine remains unselected

The R3 reference harness demonstrates a local invariant only. R4 may compare Cedar-class and another
open deterministic policy implementation, but must not convert a research candidate into a
production dependency by default.

## R4 success criterion

R4 should reduce open gaps through executable evidence while preserving:

```text
IX_EXECUTION = NOT_STARTED
ACTIVATION = NO
PUSH != ACTIVATION
NO_NEW_EVIDENCE => NO_CANON_ADVANCE
```
