# NEXUS OMEGA — QWEN-CODER R5 START HERE

```text
OBJECT = NEXUS_OMEGA_QWEN_CODER_R5_GITHUB_NATIVE_START_20260831_R0
STATE = AUTHORIZED_GITHUB_NATIVE_R5_RESEARCH_START
FROM = OPERATOR_ALEXANDER_VIA_AXIOM
TO = QWEN-CODER
CLAIM = C1_DESCRIPTIVE_ONLY
CLAIM_CEILING = C1
CLAIM_PROMOTION = FALSE
FOUNDATION_PROMOTION = FALSE
INTEGRATION_AUTHORITY = NONE
REPOSITORY_WRITE_AUTHORITY = NONE
PUBLIC_GITHUB_WRITE_BY_QWEN = NO
AUTO_FOLLOW_ON = NO
WORK_CONSERVING = YES
BLOCKED_LANE_NOT_BLOCKED_SYSTEM = ACTIVE
```

## 1. Why this GitHub-native start object exists

QWEN-CODER cannot receive file attachments through the chat transport, but has full read access to this GitHub repository. Therefore this branch is the canonical R5 transport surface for QWEN-CODER.

The earlier local foundation and maximum-progress order remain historical physical objects with these identities:

```text
LOCAL_R5_FOUNDATION_FILENAME = NEXUS_OMEGA_GITHUB_FRAMEWORK_R4_CLOSURE_R5_CANONICAL_FOUNDATION_20260831_R0.md
LOCAL_R5_FOUNDATION_BYTES = 35019
LOCAL_R5_FOUNDATION_SHA256 = fa319c72c93185a3d40f091f74f96b98098a059d61bb17f80a019a273182e317

LOCAL_R5_MAX_ORDER_FILENAME = NEXUS_OMEGA_QWEN_CODER_R5_MAXIMUM_PROGRESS_END_TO_END_ORDER_20260831_R0.md
LOCAL_R5_MAX_ORDER_BYTES = 27944
LOCAL_R5_MAX_ORDER_SHA256 = 15d9d3f2016f50d383f40d2b8fdfe1d3e6b6b6e48e8816ee54680d89c43e162d
```

Those local files are NOT claimed to be byte-identical to this GitHub-native derivative. This file is a new transport object created because the receiving agent cannot access chat attachments.

Do not reconstruct the old local files and call the reconstruction the original.

## 2. Frozen public baseline

Repository:

`nexusomegac27/NEXUS-OMEGA-EVIDENCE`

Expected R5 frozen base:

```text
MAIN = bc312c4d2cd6afa579db1f077c88d8fe08fc9470
TREE = c8856faa74efa5770717d2d44dc7d51e4269cb22
PARENT = bb1f3fd88db79d295077062895574c6d90b390bf
```

Before substantive work, independently retrieve current `main` and distinguish:

```text
FROZEN_R5_BASE
CURRENT_LIVE_MAIN
```

If current `main` differs, set `LIVE_MAIN_DRIFT=YES`; do not silently rebase the frozen research foundation.

## 3. R4 closure state

R4 is closed as a research phase.

```text
R4_STATE = CLOSED_AS_RESEARCH_PHASE
R4_PRIMARY_ADVANCE = OBJECT_IDENTITY_AND_PROVENANCE_DOMAIN_SEPARATION
R4_SECONDARY_ADVANCE = MONOTONIC_RECEIPT_AND_STATUS_ARCHITECTURE
R4_PLATFORM_ADVANCE = HASH_BOUND_PUBLIC_RESEARCH_BRANCH_OPERATIONALLY_ESTABLISHED
R4_MAIN_INTEGRATION = NO
R4_CLAIM_PROMOTION = NO
```

Public R4 research branch:

```text
BRANCH = axiom/m50grn-r0-20260831
HEAD = ed8e005c92b7c0ba647856f4f86bb29f6638d95d
PARENT = bc312c4d2cd6afa579db1f077c88d8fe08fc9470
```

Treat that branch as research evidence, not as `main`.

## 4. R5 foundational scientific separations

Never collapse these predicates:

```text
HASH_MATCH != SCIENTIFIC_VALIDITY
IDENTITY_MATCH != CONTENT_VALIDATION
SIGNATURE != SCIENTIFIC_TRUTH
CI_PASS != CLAIM_PROMOTION
ATTESTATION != SCIENTIFIC_TRUTH
AGENT_CONSENSUS != EVIDENCE
PUBLICATION != AUTHORITY
PRODUCER_RETURN != INDEPENDENT_VALIDATION
```

Permanent doctrine:

```text
BLOCKED_LANE != BLOCKED_SYSTEM
PHYSICAL_BYTES_BEFORE_SEMANTICS
MISSING_EVIDENCE_FAILS_CLOSED
CORRECTIONS_APPEND
HISTORY_IS_NOT_SILENTLY_REWRITTEN
NO_SOURCE_OBJECT_SUBSTITUTION
NO_INVENTED_PROVENANCE
```

## 5. R5 object identity model

R5 begins from a typed object graph, not a single generic file/hash pair.

Minimum object roles:

```text
outer_delivery_object
inner_source_object
source_artifact
manifest_file
manifest_projection
communication_record
ledger_index_line
ledger_head
handoff_envelope
physical_receipt
semantic_receipt
platform_receipt
attestation_subject
attestation_payload
release_asset
git_commit
git_tree
git_blob
external_witness
```

R4 demonstrated the real distinction:

```text
OUTER_DELIVERY_OBJECT != INNER_SOURCE_OBJECT
```

and therefore:

```text
CORRECT_DIGEST + WRONG_OBJECT_ROLE = PROVENANCE_FAILURE
```

## 6. Typed digest domains

Minimum R5 digest domains:

```text
OUTER_DELIVERY_BYTES
SOURCE_ARTIFACT_BYTES
MANIFEST_FILE_BYTES
JSON_MANIFEST_PROJECTION
COMMUNICATION_RECORD_BYTES
LEDGER_INDEX_LINE_BYTES
HANDOFF_ENVELOPE_BYTES
ATTESTATION_SUBJECT_BYTES
ATTESTATION_PAYLOAD_BYTES
RELEASE_ASSET_BYTES
GIT_OBJECT_ID
```

Hard rule:

```text
SAME_HASH_ALGORITHM != SAME_DIGEST_DOMAIN
```

A digest descriptor should bind:

```text
domain
object_role
algorithm
digest
bytes
media_type
locator
canonicalization_id
source_ref
status
```

## 7. Representation and canonicalization

Distinguish:

```text
PHYSICAL_BYTES_HASH = hash(raw physical bytes)
REPRESENTATION_HASH = hash(canonical representation)
GIT_OBJECT_ID = Git object identity
ATTESTATION_SUBJECT_DIGEST = digest under the attestation subject contract
```

Historical `NEXUS_SORTED_JSON_V1` must remain preserved. It is not full RFC 8785 JCS.

RFC 8785/JCS may only be claimed where:

```text
OBJECT_TYPE = JSON
CANONICALIZATION_ID = explicit
TEST_VECTORS = verified
INTEROPERABILITY = independently tested
```

Do not apply JCS to ZIP/binary artifact identity.

## 8. Manifest self-binding research target

Design a non-circular identity projection:

```text
MANIFEST
→ SELECT VERSIONED IDENTITY PROJECTION
→ EXCLUDE SELF-DIGEST FIELD
→ APPLY EXPLICIT CANONICALIZATION
→ HASH PROJECTED BYTES
→ STORE manifest_identity_sha256
```

Required contract fields:

```text
projection_id
projection_version
source_schema_version
included_fields
excluded_fields
canonicalization_id
hash_algorithm
output_digest_field
failure_codes
```

The projection must be independently implementable and tested.

## 9. Receipt separation

Formalize three independent receipt types:

```text
PHYSICAL_RECEIPT
SEMANTIC_REVIEW_RECEIPT
PLATFORM_CONTROL_RECEIPT
```

Physical receipt may establish byte identity and presence only.
Semantic receipt may establish bounded interpretation, evidence/counterevidence and uncertainty.
Platform receipt may establish repository/branch/commit/tree/ruleset/check/release/attestation state.

None may silently inherit the claims of another receipt type.

## 10. E0–E3 model

```text
E0_OBJECT = physical bytes + locator
E1_REPRESENTATION = encoding + canonicalization + digest + projection
E2_GOVERNANCE = receipt + handshake + branch + commit + ruleset + attestation
E3_INTERPRETATION = claim ceiling + uncertainty + scientific relevance
```

Required separations:

```text
E0_MATCH != E3_VALIDITY
E1_DIGEST_MATCH != E3_TRUTH
E2_RULESET_ACTIVE != E0_BYTE_IDENTITY
E2_ATTESTATION_VALID != E3_SCIENTIFIC_TRUTH
```

## 11. Monotonic status algebra

Model status independently across:

```text
IDENTITY
REPRESENTATION
PROVENANCE
STRUCTURE
SEMANTICS
INDEPENDENCE
PLATFORM
ARCHIVAL
```

Prevent unsupported transitions such as:

```text
SOURCE_NOT_PRESENT → MATCH
INDETERMINATE → MATCH
MISMATCH → PASS
PASS_WITH_CAVEATS → PASS
SELF_VALIDATED → INDEPENDENTLY_VALIDATED
UNSIGNED → SIGNED
NOT_ATTESTED → ATTESTED
```

without a new evidence object and explicit transition receipt.

## 12. Failure-code architecture

Create deterministic, domain-specific failures rather than generic hash errors. At minimum cover:

```text
SOURCE_OBJECT_SUBSTITUTION
BOUND_ARTIFACT_DIGEST_MISMATCH
DIGEST_DOMAIN_SUBSTITUTION
OBJECT_ROLE_SUBSTITUTION
OUTER_INNER_IDENTITY_MISMATCH
LOCATOR_MISMATCH
SUBJECT_MISMATCH
MANIFEST_SCOPE_ERROR
MANIFEST_CANONICALIZATION_ERROR
MANIFEST_PROJECTION_AMBIGUITY
SUBJECT_DIGEST_DOMAIN_ERROR
ATTESTATION_PAYLOAD_DOMAIN_ERROR
ATTESTATION_POLICY_FAILURE
PHYSICAL_RECEIPT_SEMANTIC_OVERREACH
PLATFORM_RECEIPT_SCIENTIFIC_OVERREACH
NON_MONOTONIC_STATUS_PROMOTION
CANONICALIZATION_ID_MISSING
PROJECTION_CONTRACT_MISSING
INVALID_PARENT_BIND
RECONSTRUCTED_ORIGINAL_MISREPRESENTATION
UNAUTHORIZED_CLAIM_PROMOTION
UNAUTHORIZED_MAIN_MUTATION
```

## 13. GitHub platform research

Current intended logical validation lanes are:

```text
validate-anchor
validate-scientific-communication
validate-scientific-ledger
validate-a83-framework-hardening
```

The current Ruleset uses required context `validate` while multiple workflows use the same job/check name.

Research whether this establishes four distinct enforced gates.

Conservative state unless independently demonstrated:

```text
FOUR_LOGICAL_CI_WORKFLOWS = ESTABLISHED
FOUR_DISTINCT_PLATFORM_REQUIRED_GATES = NOT_ESTABLISHED
```

Do not modify GitHub Rulesets under this order.

## 14. Attestation model

Formalize separately:

```text
SUBJECT
SUBJECT_DIGEST
PREDICATE_TYPE
PREDICATE
ISSUER_OR_SIGNER
SIGNATURE_OR_CERTIFICATE
TRANSPARENCY_OR_PLATFORM_RECEIPT
POLICY
VERIFICATION_RESULT
SCIENTIFIC_INTERPRETATION
```

A valid attestation may strengthen provenance assurance. It may not promote a scientific claim.

## 15. Ledger research

Separate:

```text
CONCURRENT_SERIALIZATION
CRASH_ATOMICITY
DURABILITY
MULTI_HOST_COORDINATION
NFS_SEMANTICS
GIT_CONFLICT_CONTROL
```

Model crash points around:

```text
OBJECT_WRITE
INDEX_WRITE
LATEST_WRITE
FSYNC
LOCK_RELEASE
```

Evaluate candidate designs such as:

```text
write-ahead journal
transaction marker
single immutable append journal + derived head
content-addressed staging + commit marker
SQLite / transactional backend
single-writer service
CAS / compare-and-swap
Git-native serialization
```

Do not claim distributed safety from local `flock`.

## 16. Maximum-progress mandatory work packages

Execute all unblocked packages:

```text
WP-A FOUNDATION / LIVE-STATE VERIFICATION
WP-B OBJECT ROLE TYPE SYSTEM
WP-C TYPED DIGEST DOMAIN SYSTEM
WP-D MANIFEST SELF-BINDING PROJECTION
WP-E RFC8785/JCS INTEROPERABILITY HARNESS
WP-F RECEIPT TYPE SYSTEM
WP-G MONOTONIC STATUS ALGEBRA
WP-H FAILURE CODE REGISTRY
WP-I PROVENANCE RELATION GRAPH
WP-J THREAT MODEL / ADVERSARIAL MATRIX
WP-K ATTESTATION SUBJECT / PREDICATE MODEL
WP-L GITHUB REQUIRED-CHECK IDENTITY MODEL
WP-M LEDGER CONCURRENCY / CRASH-CONSISTENCY MODEL
WP-N SCHEMA / VALIDATOR REFERENCE IMPLEMENTATION
WP-O PROPERTY-BASED / DIFFERENTIAL TESTING
WP-P NEGATIVE FIXTURE EXPANSION
WP-Q IMPLEMENTATION TRACEABILITY
WP-R MIGRATION / BACKWARD COMPATIBILITY
WP-S PERFORMANCE / COMPLEXITY BOUNDS
WP-T SECURITY / PRIVACY REVIEW
WP-U PRIORITIZED R6 ROADMAP
```

Do not stop the whole run because one WP is blocked.

## 17. Adversarial test target

Use classes:

```text
A REPRESENTATION_ERRORS
B REPOSITORY_RELEASE_RISKS
C DUAL_LAYER_COMPOSITION
D HANDSHAKE_STATUS_ALGEBRA
E COMBINED_HYPOTHETICAL_ATTACKS
F OBSERVABILITY_MEASUREMENT_LIMITS
G MULTIWRITER_CRASH_CONSISTENCY
H DIGEST_DOMAIN_CONFUSION
I ATTESTATION_SUBJECT_CONFUSION
J CANONICALIZATION_INTEROPERABILITY
K PLATFORM_CHECK_IDENTITY_COLLISION
L RECEIPT_ROLE_CONFUSION
```

Target:

```text
>= 120 adversarial cases
>= 150 R5 negative fixtures
```

Every case must identify prediction, expected failure code, observable result, falsification condition and claim scope.

A hypothetical attack is not an observed defect.

## 18. Reference implementation expectations

Where formal contracts are stable enough, create local candidate namespaces such as:

```text
r5/
schema/r5_*
scripts/r5_*
tests/r5_*
validation/r5_*
```

Preferred candidate components:

```text
object-role registry
digest-domain registry
manifest-projection validator
physical receipt schema
semantic receipt schema
platform receipt schema
status-transition validator
failure-code registry
provenance-graph validator
attestation-subject validator
```

Preserve historical v1/A83 bytes unless a change is strictly required and explicitly documented.

## 19. Property and differential testing

Useful required properties include:

```text
REORDERED_JSON_OBJECT_KEYS_CANONICALIZE_EQUAL
PHYSICAL_BYTES_CHANGE_DIGEST
DIGEST_DOMAIN_CHANGE_REJECTS_SUBSTITUTION
ILLEGAL_STATUS_TRANSITION_ALWAYS_REJECTED
RECEIPT_TYPE_OVERREACH_ALWAYS_REJECTED
PROJECTION_SELF_DIGEST_EXCLUDED
PROJECTION_CHILD_MUTATION_CHANGES_DIGEST
GRAPH_INVALID_EDGE_REJECTED
```

Use differential JCS implementations where feasible and record reproducible seeds.

## 20. Primary-source hierarchy

Use:

```text
P0_NORMATIVE_PRIMARY
P1_IMPLEMENTATION_PRIMARY
P2_SECONDARY_ANALYSIS
P3_EXPLORATORY
```

At minimum independently inspect primary sources for:

```text
RFC 8785
JSON Schema Draft 2020-12
GitHub Rulesets / Required Status Checks
GitHub Artifact Attestations
in-toto Attestation Framework
SLSA provenance
Sigstore / Rekor
SCITT
RFC 3161
Git object model
```

Source quality and direct relevance outrank source count.

## 21. Security and privacy

Review at least:

```text
path traversal
symlink escape
duplicate normalized paths
Unicode confusables
low-entropy private-data hashing
receipt replay
record replay
locator substitution
digest substitution
schema confusion
algorithm downgrade
attestation issuer confusion
dependency compromise
malicious fixture
CI check-name collision
Git history manipulation
```

Never expose secrets or private data.

## 22. Backward compatibility

R5 must coexist with and preserve:

```text
NEXUS_SORTED_JSON_V1
existing communication records
existing A83 handoff artifacts
historical A84 witness
existing SHA256 values
existing Git commits
```

Evaluate version dispatch, legacy adapters, dual-read/single-write and explicit supersession receipts.

```text
NEW_FORMALISM != RETROACTIVE_INVALIDATION_OF_HISTORICAL_BYTES
```

## 23. R6 roadmap requirement

Produce a concrete prioritized R6 roadmap:

```text
P0 BLOCKING CORRECTNESS
P1 HIGH-VALUE ROBUSTNESS
P2 INTEROPERABILITY
P3 ARCHIVAL / PLATFORM HARDENING
P4 LONG-TERM SCALE
```

For every item provide problem, evidence, impact, proposed solution, falsification test, effort, dependencies and recommended next validator.

## 24. No public write by QWEN-CODER

Under this R5 order QWEN-CODER may research, fetch public sources, clone/read repository state and build/test local candidates.

It may NOT:

```text
push
create PR
merge
mutate main
mutate Rulesets
create release
tag
publish attestation
force push
```

If publication appears ready, report:

```text
PUBLICATION_READY_CANDIDATE = YES
```

and stop at the return gate.

## 25. Required output family

Return physically durable artifacts, preferably as repository-independent UTF-8 files plus candidate patch where relevant:

```text
NEXUS_OMEGA_QWEN_CODER_R5_MAXIMUM_PROGRESS_RETURN_20260831_R0.md
NEXUS_OMEGA_QWEN_CODER_R5_OPERATOR_AXIOM_HANDSHAKE_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_ARTIFACT_BINDING_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_IMPLEMENTATION_TRACEABILITY_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_FAILURE_CODE_REGISTRY_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_OBJECT_ROLE_REGISTRY_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_DIGEST_DOMAIN_REGISTRY_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_STATUS_ALGEBRA_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_THREAT_MATRIX_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_NEGATIVE_FIXTURES_20260831_R0.jsonl
NEXUS_OMEGA_QWEN_CODER_R5_PRIMARY_SOURCE_REGISTER_20260831_R0.json
NEXUS_OMEGA_QWEN_CODER_R5_R6_ROADMAP_20260831_R0.md
NEXUS_OMEGA_QWEN_CODER_R5_SHA256SUMS_20260831_R0.txt
```

If candidate code is produced also return patch, patch inventory, exact test results, canonicalization vectors and provenance graph fixtures.

For every material output:

```text
WRITE → CLOSE → READ_BACK → BYTE_COUNT → SHA256 → VERIFY_EXISTS → EXPOSE
```

## 26. Producer/validator boundary

QWEN-CODER is producer of its R5 candidate.

Therefore terminal status must preserve:

```text
INDEPENDENT_R5_VALIDATION = NOT_YET_EXECUTED
```

unless a truly separate external validator is used with independent source identity and role separation.

Do not simulate independence with another internal persona.

## 27. Allowed terminal states

Use one primary terminal state:

```text
PASS_R5_RESEARCH_AND_LOCAL_IMPLEMENTATION_WITH_CAVEATS_C1
PASS_R5_RESEARCH_DESIGN_ONLY_WITH_CAVEATS_C1
FAIL_MAJOR_R5_FORMALIZATION_INCONSISTENT_C1
HARD_FAIL_R5_FOUNDATION_BIND_MISMATCH_C1
INCOMPLETE_R5_REQUIRED_SOURCE_NOT_PRESENT_C1
HOLD_R5_RETURN_TRANSPORT_NOT_ESTABLISHED_C1
```

## 28. Scientific objective

Do not optimize for a positive verdict.

Attempt to break:

```text
OBJECT_ROLE_SEPARATION
DIGEST_DOMAIN_SEPARATION
MANIFEST_PROJECTION
RECEIPT_ROLE_SEPARATION
STATUS_MONOTONICITY
ATTESTATION_SUBJECT_BINDING
GITHUB_CHECK_IDENTITY
LEDGER_CRASH_MODEL
CANONICALIZATION_INTEROPERABILITY
```

If a prior R4 assumption is wrong, correct it explicitly.
If a design is untestable, reject it.
If local tests succeed but broader claims are unsupported, keep the narrower claim.

```text
R5_GOAL = MAXIMUM_REPRODUCIBLE_PROGRESS_WITHOUT_CLAIM_PROMOTION
NEXT_ACTION = RETURN_TO_AXIOM_FOR_INDEPENDENT_REHASH_AND_R5_ADJUDICATION
```
