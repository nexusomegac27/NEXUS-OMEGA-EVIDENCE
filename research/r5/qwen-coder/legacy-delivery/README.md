# Legacy QWEN-CODER R5 delivery surface

Byte-preserving Git-blob reuse from historical branch `axiom/r5-qwen-coder-start-20260831`.

This directory preserves the earlier Base64-chunk transport, its manifest, delivery receipt, remote-rehash expectations, status, and start instructions. It is historical transport evidence, not the current live integration surface.

No file in this directory is semantically promoted by relocation.
