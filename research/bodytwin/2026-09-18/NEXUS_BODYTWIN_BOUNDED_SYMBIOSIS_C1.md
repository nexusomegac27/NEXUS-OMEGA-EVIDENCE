# NEXUS OMEGA — BodyTwin Bounded Symbiosis — Public C1 Derivative

```text
OBJECT = NEXUS_OMEGA_BODYTWIN_BOUNDED_SYMBIOSIS_PUBLIC_C1_20260918_R0
STATE = BOUNDED_INTEGRATION_C1
CLAIM = C1_DESCRIPTIVE_ONLY
SOURCE_PACKAGE_SHA256 = bc7957e0670f42e8acfaf54884a3e227491abe60dd9fede42772a3c6e48979af
SYMBIOSIS = AUTHORIZED_BOUNDED
H3_TRACK_B = RETAINED
BODYTWIN_VALIDATED = NO
```

## 1. Symbiosis boundary

The visual-engineering lane and the governance/representation lane remain distinct.

### Visual realization lane

```text
CAPTURE
→ RECONSTRUCTION
→ IDENTITY / GEOMETRY CORE
→ MOTION / TEMPORAL STATE
→ GENERATIVE VISUAL APPROXIMATION
→ METRIC / FALSIFICATION
```

This lane contains the previously retained H3 / Track-B research direction. No end-to-end validation is claimed here.

### Governance and neutral representation lane

```text
CONSENT
→ PROVENANCE
→ REPRESENTATION CLASSIFICATION
→ UNCERTAINTY
→ STYLE SEPARATION
→ REVIEW / PUBLICATION CONTROL
```

This lane governs how outputs are described and released. It does not determine scientific truth.

## 2. Representation classes

```text
L0_OBSERVED
=
directly captured evidence and measurements

L1_RECONSTRUCTED
=
geometry or appearance reconstructed from bound observations

L2_GENERATED_APPROXIMATION
=
model-created detail or refinement not directly observed

L3_STYLE_DERIVATIVE
=
explicitly noncanonical visual transformation

UNKNOWN_UNOBSERVED
=
must remain explicit and must not silently become observed fact
```

Priority:

`L0_OBSERVED > L1_RECONSTRUCTED > L2_GENERATED_APPROXIMATION > L3_STYLE_DERIVATIVE`

## 3. Neutrality invariants

A canonical BodyTwin candidate must not silently introduce:

- beautification;
- age shift;
- face slimming or symmetrization;
- body idealization;
- skin smoothing;
- eye enlargement;
- muscle enhancement;
- sexualization;
- invented scars, hairline, anatomy, emotion, personality, health state, or identity facts.

Observable asymmetry, wrinkles, scars, freckles, posture, hair irregularity and clothing behavior should be preserved when they are actually captured and when their use is consent-bound.

## 4. Style layer

The package's 16-frame style artifact is classified only as:

`NONCANONICAL_LAYER_VARIABILITY_DEMONSTRATOR`

It is not evidence that identity was preserved. It is not evidence that named styles were actually implemented. It is not scientific validation.

## 5. Presentation MP4

The package MP4 is classified:

```text
MP4_CONTAINER = PASS
MP4_VISUAL_LEGIBILITY = FAIL
MP4_CANONICAL_STATUS = EXCLUDED_FROM_EVIDENCE_PATH
```

No scientific or identity-preservation claim may depend on it.

## 6. Scientific metric lane retained

Future BodyTwin trials must independently bind and calibrate metrics such as:

- identity-similarity proxy metrics;
- temporal identity drift;
- VBench / DINO subject consistency as a secondary non-biometric measure;
- temporal LPIPS;
- optical-flow warp consistency;
- geometry / landmark consistency where valid;
- neutrality and hallucination audit;
- negative controls;
- blinded human evaluation.

`METRIC_CODE_RUNS != METRIC_CALIBRATED != BODYTWIN_VALIDATED`

## 7. Consent and publication

`CONSENT_BIND = OPEN`

Therefore this public derivative does not publish human reference imagery or biometric media from the package. A later consent receipt must bind subject, purpose, permitted transformations, retention, publication scope, revocation semantics and derived-artifact handling before a human-subject trial or public identity release.

## 8. Current terminal state

```text
SYMBIOSIS = AUTHORIZED_BOUNDED
VISUAL_REALIZATION = H3_TRACK_B_RETAINED
NEW_PACKAGE_ROLE = GOVERNANCE_AND_NEUTRAL_REPRESENTATION_LAYER
BODYTWIN_VALIDATED = NO
TRIAL = NO
PUBLIC_IDENTITY_RELEASE = HOLD
CLAIM_PROMOTION = NO
CLAIM_CEILING = C1_DESCRIPTIVE_ONLY
```
