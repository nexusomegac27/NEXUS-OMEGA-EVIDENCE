# NEXUS OMEGA — CURSOR FULL BUNDLED RESEARCH INTAKE, A90 COMPLETION & GITHUB BRIDGE ORDER

```text
OBJECT = NEXUS_OMEGA_CURSOR_FULL_BUNDLED_RESEARCH_INTAKE_A90_R5_GITHUB_BRIDGE_ORDER_20260901_R0
PACKAGE_ID = NEXUS_RP_20260901T035600Z_retroactive-symbiotic-backfill_6f1a9c27
SNAPSHOT = v2
PREDECESSOR_SNAPSHOT = v1
FROM = OPERATOR_ALEXANDER_VIA_AXIOM
TO = CURSOR_PRAXIS
AUTHORITY = OPERATOR_ALEXANDER_VIA_AXIOM
CLAIM_CEILING = C1
CURSOR_ACTION = START_AUTHORIZED
STRAND_STATE = REOPENED_FOR_BUNDLED_RESEARCH_PROCESSING
WORK_CONSERVING = YES
MAXIMUM_BUNDLED_ORDER = YES
INTEGRATION_AUTHORITY = CONDITIONAL_AND_PATH_SCOPED
CLAIM_PROMOTION = NO
GATE_START = NO_UNLESS_SEPARATELY_AUTHORIZED
DEPLOY = NO
FINAL_PATH_SELF_LEARNING = NO
QUARANTINE_BYPASS = NO
SOURCE_OBJECT_SUBSTITUTION = PROHIBITED
```

## 0. Causal activation

This order is the explicit intake that Cursor's preceding ACK requested.

Predecessor ACK:
`NEXUS_OMEGA_CURSOR_ACK_AXIOM_RESEARCH_LANES_AND_REPOSITORY_ORDER_LAW_20260901_R0`

Predecessor state:
`STRAND_AT_REST_PENDING_BUNDLED_RESEARCH_INTAKE`

This snapshot changes only the processing authorization:

```text
STRAND_AT_REST_PENDING_BUNDLED_RESEARCH_INTAKE
→ READY_FOR_CURSOR_V2
→ CURSOR_PROCESSING
```

It does not rewrite the predecessor ACK, v1, or any earlier receipt.

## 1. Live public repository anchors — AXIOM re-verified

```text
REPOSITORY = nexusomegac27/NEXUS-OMEGA-EVIDENCE
LIVE_MAIN = 7c93e73c0e39a6604b4c00996b5b04c3ef413b4e
LIVE_MAIN_TREE = d2d2b53eea95a747fb3a62ff63a4bb601849ff3a
MAIN_SIGNATURE = VERIFIED_VALID

AXIOM_R5_FORMALIZATION_BRANCH = axiom/r5-formalization-research-20260831
AXIOM_R5_FORMALIZATION_HEAD = eaf6c3ad95c8bf021aeaf959752e56e77128ef0b
AXIOM_R5_FORMALIZATION_TREE = 8be893f6744cb34aaeeaaa4781129de547abb1e9
AXIOM_R5_FORMALIZATION_SIGNATURE = UNSIGNED_RESEARCH_BRANCH
```

Cursor MUST re-read live `main` at execution time. If it moved, preserve `7c93e73c0e39a6604b4c00996b5b04c3ef413b4e` as the v2 planning anchor and record the new live head separately.

## 2. Inherited v1 mission remains active

Snapshot v2 does not cancel or duplicate v1. All still-unprocessed v1 duties remain active, including:
- read-only recursive physical inventory of registered local roots;
- streaming SHA-256;
- archive safety/inventory;
- Git repository identity capture;
- timestamp evidence;
- causal chronology;
- deduplication/lineage;
- NEEDLE V1–V6 reconstruction where source bytes are available;
- persistent GitHub-memory layer reconstruction;
- self-learning-agent synthesis;
- GitHub reconciliation;
- private/public-safe split;
- no source-root mutation.

Where v2 gives more specific instructions, v2 controls prospectively.

## 3. Repository Order Law — mandatory intake gate

Before every repository write:

```text
CLASSIFY_ARTIFACT
→ DETERMINE_LIFECYCLE
→ DETERMINE_OWNING_DOMAIN
→ VERIFY_TARGET_PATH
→ VERIFY_SOURCE_BINDING
→ ONLY_THEN_WRITE
```

Routing:

```text
ACTIVE_RESEARCH
→ research/r5/<agent-or-topic>/

INTEGRATION_GRADE
→ docs/r5/<topic>/
→ schema/r5/<topic>/
→ scripts/r5/<topic>/
→ tests/r5/<topic>/
→ validation/r5/<topic>/
→ examples/r5/<topic>/
```

Prohibited:
`ROOT_DUMPS`, loose receipts at root, cosmetic movement of historically bound A83 files, silent replacement of producer returns.

## 4. Priority processing lanes

Process in the following dependency-aware order. Independent lanes may run concurrently where safe.

### LANE A — v2 snapshot and GitHub bridge
1. verify every file in this snapshot against `SHA256SUMS.txt`;
2. post GitHub PR ACK using `GITHUB_BRIDGE_PROTOCOL.md`;
3. create a canonical `CURSOR_PROCESSING` event;
4. record Cursor local GitHub read/write capability independently:
   - GitHub platform read;
   - GitHub write through authenticated method;
   - local `origin` availability;
   - local push ability;
   - PR/comment ability;
5. preserve:
   `GITHUB_PLATFORM_ACCESS != LOCAL_GIT_REMOTE_ACCESS`.

### LANE B — local research roots, complete byte-bound intake
Scan all roots already registered by v1 and explicitly re-mentioned by Cursor:
- `NEXUS_NEEDLE_RANK_V4`
- `NEXUS_NEEDLE_RANK_V5`
- `NEXUS_NEEDLE_RANK_V6`
- persistent GitHub communication/memory layer base
- generations 2–5
- `NEXUS_self-learning-agent`
- `NEXUS_self-learning-agent 1`
plus the remaining v1-registered roots.

For every source:
- do not infer byte identity from folder name;
- hash outer archives before members;
- safely enumerate nested archives;
- detect exact duplicate and derivative relations;
- classify old, current, superseded, and reference-only material.

### LANE C — R5 AXIOM formalization semantic intake
Process `axiom/r5-formalization-research-20260831@eaf6c3ad95c8bf021aeaf959752e56e77128ef0b` independently.

Required candidate families include:
- primary source register;
- object-role registry;
- digest-domain registry;
- status algebra;
- failure-code registry;
- receipt type system;
- provenance graph;
- attestation/transparency profile;
- manifest self-binding profile;
- ledger concurrency/crash-consistency analysis.

Cursor duties:
1. bind every candidate file/commit/blob;
2. compare with current `main`;
3. identify implementable vs research-only portions;
4. independently test material implementation claims;
5. preserve `NEXUS_SORTED_JSON_V1 != RFC8785_JCS`;
6. treat static ledger race/crash findings as static findings until reproduced;
7. generate a traceability matrix:
   `candidate → current implementation → test → verdict → integration eligibility`.

No candidate becomes stable solely because AXIOM authored it.

### LANE D — QWEN-CODER R5 material
Process all physically available QWEN-CODER-R5 producer/validation artifacts that v1 registered or Cursor can now locate.

Distinguish:
- original producer bytes;
- GitHub-native derivatives;
- transport containers;
- validation orders;
- independent returns;
- source-not-present conditions.

Never reconstruct a missing QWEN return from summaries.

### LANE E — A90 Self-Learning Sandbox M1R1
This snapshot includes:
- `A90_PLAN_BINDING.md`
- `A90_AXIOM_COMPLETION_STATE.json`.

Independent starting state:

```text
A90_M1_COMPLETION = NOT_ESTABLISHED
C1_1_ARTIFACT_STATUS = NOT_ACHIEVED
GROK_FAIL_WITH_MAJOR_REVISIONS = PLAUSIBLE_PENDING_PHYSICAL_BIND
```

#### E1 — physically bind the current A90 source collection
Locate the operator's actual `Nexus_Self_Learnig_Agent.zip` / corresponding local research source.

AXIOM previously encountered a runtime mount failure and explicitly did not substitute another ZIP.

Cursor must:
- hash outer bytes;
- safe-list all members;
- recursively hash nested archives;
- compare actual inventory with GROK's reported inventory;
- classify each member:
  `HISTORICAL_PRIOR`,
  `RESEARCH_SOURCE`,
  `PRODUCER_WORKSPACE`,
  `GENERIC_PROVIDER_SCAFFOLD`,
  `A90_REQUIRED_INPUT`,
  `NON_CANON_REFERENCE`.

#### E2 — adjudicate GROK's reported A90 gaps
Verify, do not assume:
1. missing A90 `checksums.sha256`;
2. missing/unpopulated `outputs/audit_report.json`;
3. absent or non-runnable isolated `scripts/05_reproduce_all.py`;
4. collection ZIP rather than one isolated completion package.

#### E3 — produce/validate isolated A90-M1R1 completion package
Target:
`A90_M1R1_ISOLATED_REPRODUCIBLE_SELF_LEARNING_SANDBOX_COMPLETION`.

The learning system MUST be a provider-neutral candidate compiler:

```text
LEARNER_OUTPUT
→ QUARANTINE_CANDIDATE
→ INDEPENDENT_EVALUATION
→ INDEPENDENT_REPLAY
→ ADJUDICATION
→ OPTIONAL_GOVERNED_INTEGRATION
```

Never:
`LEARNER_OUTPUT → FINAL_PATH`.

Required invariants:
- learner cannot edit evaluator;
- learner cannot edit holdout;
- learner cannot edit acceptance threshold;
- learner cannot remove quarantine;
- learner cannot self-mark independent pass;
- rejected candidate lineage remains visible;
- candidate IDs are content/provenance bound;
- provider identity is not governance authority.

#### E4 — reproduction
At minimum:
- complete `scripts/00_...` through `05_reproduce_all.py`;
- dependency lock;
- schemas;
- manifest;
- complete generated outputs;
- member checksums;
- two clean-environment runs;
- explicit deterministic/non-deterministic field policy;
- soft-overclaim scan.

#### E5 — independent adversarial validation
Run at least the 32 fixture classes defined by AXIOM's A90 plan, including:
- integrity corruption;
- missing scripts/outputs;
- self-authorization attempts;
- evaluator/test mutation;
- hidden-answer access;
- internet benchmark lookup;
- grader gaming;
- reward hacking;
- descendant contamination;
- rollback contamination;
- aggregate-score masking of regressions.

#### E6 — exact claim boundary
Only after independent success may this artifact label become eligible:

`C1.1_SELF_LEARNING_SANDBOX_ARTIFACT_VALIDATED_STRUCTURE_ONLY`

It remains inside:

`CLAIM_CEILING = C1`.

It proves structure/replay only, not autonomous safe improvement or learning efficacy.

### LANE F — NEEDLE V4/V5/V6
Use V4's already reported GROK-R4-rehash status as a hypothesis to re-bind, not as a shortcut.

For V4→V5→V6:
- recover exact predecessor relationships;
- identify hypothesis changes;
- ranking method changes;
- accepted/rejected candidates;
- negative results;
- source substitutions;
- rehashes;
- unresolved gaps.

Do not collapse semantically similar versions.

### LANE G — persistent GitHub memory/communication generations
For base through generation 5:
- classify generation vs duplicate;
- compare local bytes to public GitHub;
- extract communication schema evolution;
- determine what should remain local/private;
- identify reusable protocol/schema components;
- map to `research/pipeline` without treating narrative memory as evidence.

Key design principle:
GitHub should become the durable communications substrate, but **the repository must preserve evidence-role separation**.

## 5. GitHub communication transition

Cursor is now explicitly authorized to prefer GitHub over operator-mediated long-message transport.

### Live transport
Use the PR opened for this v2 handoff.

### Canonical persistence
Use `research/pipeline/`.

### Cursor-side event sequence
After snapshot verification:

```text
READY_FOR_CURSOR
→ CURSOR_PROCESSING
```

At meaningful checkpoints, write new immutable events, not mutable status files.

At terminal:
```text
CURSOR_PROCESSING
→ PROCESSED
```
only when a terminal processing return exists.

## 6. Integration policy

This order authorizes research processing and candidate remediation, not blanket stable integration.

Cursor MAY:
- create research branches;
- create candidate patches;
- execute fresh-VM tests;
- produce independent receipts;
- open PRs for reviewable research/pipeline artifacts;
- integrate clearly mechanical, independently tested repository-order/pipeline corrections where no scientific claim is promoted.

Cursor MUST NOT without separate evidence/authority:
- promote C1 claims;
- enable A90 final path;
- remove quarantine;
- deploy;
- rewrite historical evidence;
- force-push canonical history;
- merge an unvalidated scientific candidate merely to reduce backlog.

## 7. Required consolidated outputs

Produce one coherent processing return set, preferably under a successor snapshot `v3` or a Cursor processing subtree associated with this package.

Minimum:
1. `CURSOR_PROCESSING_RETURN.md`
2. `CURSOR_PROCESSING_RECEIPT.json`
3. `PHYSICAL_FILE_INVENTORY.jsonl`
4. `ARCHIVE_MEMBER_INVENTORY.jsonl`
5. `GIT_REPOSITORY_INVENTORY.json`
6. `TIMESTAMP_EVIDENCE_REGISTER.json`
7. `DEDUPLICATION_AND_LINEAGE.json`
8. `R5_FORMALIZATION_TRACEABILITY_MATRIX.json`
9. `QWEN_R5_INTAKE_AND_VALIDATION_STATE.json`
10. `A90_PHYSICAL_BIND_AND_GAP_AUDIT.json`
11. `A90_M1R1_REPRODUCTION_RECEIPT.json` if executable package exists
12. `A90_ADVERSARIAL_FIXTURE_RESULTS.json` if execution authorized/possible
13. `NEEDLE_V4_V6_CAUSAL_SYNTHESIS.md`
14. `GITHUB_MEMORY_LAYER_GENERATIONS_SYNTHESIS.md`
15. `GITHUB_RECONCILIATION.json`
16. `PUBLIC_PRIVATE_SPLIT_REGISTER.json`
17. `UNRESOLVED_GAPS_AND_FALSIFICATION_TARGETS.md`
18. `SHA256SUMS.txt`

All final files:
`WRITE → CLOSE → READ_BACK → BYTES → SHA256 → VERIFY_EXISTS`.

## 8. Failure handling

Scoped errors do not stop independent lanes.

```text
UNREADABLE_ROOT
→ RECORD + CONTINUE

GITHUB_WRITE_FAILURE
→ RECORD + CONTINUE_LOCAL_WORK

A90_SOURCE_NOT_PRESENT
→ NO_SOURCE_SUBSTITUTION + CONTINUE_R5/NEEDLE/GITHUB_MEMORY

QWEN_RETURN_NOT_PRESENT
→ SOURCE_NOT_PRESENT + CONTINUE_OTHER_LANES
```

Only hard-stop the affected operation for:
- destructive/main mutation without authority;
- historical evidence rewrite;
- source-object substitution;
- fabricated provenance;
- claim promotion without evidence;
- A90 quarantine/final-path bypass.

## 9. Required terminal verdict

Return one of:

```text
T1_BUNDLED_RESEARCH_PROCESSED_COMPLETE_C1
T2_BUNDLED_RESEARCH_PROCESSED_WITH_MATERIAL_SOURCE_GAPS_C1
T3_BUNDLED_RESEARCH_PROCESSED_WITH_EXECUTION_CAVEATS_C1
T4_BUNDLED_RESEARCH_PARTIAL_LOCAL_ACCESS_BLOCKED_C1
T5_GLOBAL_EXECUTION_BLOCKED_C1
```

Also report per-lane terminal state for A–G.

`INVENTORY_COMPLETE`, `HASHING_COMPLETE`, `A90_PACKAGE_BUILT`, `R5_ANALYZED`, or `GITHUB_BRIDGE_ACTIVE` are nonterminal.

## 10. Expected first response from Cursor

Do not send another `STRAND_AT_REST` acknowledgement.

The correct first response is an execution ACK similar to:

```text
OBJECT = NEXUS_OMEGA_CURSOR_ACK_BUNDLED_RESEARCH_INTAKE_V2_20260901_R0
STATE = CURSOR_PROCESSING
CLAIM_CEILING = C1
PACKAGE_ID = NEXUS_RP_20260901T035600Z_retroactive-symbiotic-backfill_6f1a9c27
SNAPSHOT = v2
SNAPSHOT_SHA256SUMS = MATCH|FAIL|NOT_COMPUTABLE
LIVE_MAIN_OBSERVED = <sha>
LOCAL_SOURCE_ACCESS = <state>
GITHUB_BRIDGE = ACTIVE|READ_ONLY|UNAVAILABLE
CURSOR_ACTION = STARTED
NEXT_ACTION = EXECUTE_LANES_A_TO_G_WORK_CONSERVING
```

That ACK should be posted first to the GitHub PR conversation and then persisted as a canonical event/receipt.

# Terminal AXIOM dispatch

```text
AXIOM_DISPATCH = COMPLETE
CURSOR_MAXIMAL_ONBOARD = AUTHORIZED_BY_OPERATOR
BUNDLED_INTAKE = ACTIVE
GITHUB_FIRST_COMMUNICATION = REQUESTED
CHAT_ONLY_RELAY = DEPRECATED_TO_FALLBACK
CLAIM_CEILING = C1
```
