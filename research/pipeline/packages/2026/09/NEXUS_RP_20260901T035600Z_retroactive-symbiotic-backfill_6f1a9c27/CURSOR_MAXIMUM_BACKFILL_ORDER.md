# NEXUS OMEGA — CURSOR MAXIMUM RETROACTIVE SYMBIOTIC RESEARCH BACKFILL ORDER

```text
OBJECT = NEXUS_OMEGA_CURSOR_MAXIMUM_RETROACTIVE_SYMBIOTIC_RESEARCH_BACKFILL_ORDER_20260901_R0
PACKAGE_ID = NEXUS_RP_20260901T035600Z_retroactive-symbiotic-backfill_6f1a9c27
SNAPSHOT_INPUT = v1
FROM = OPERATOR_ALEXANDER_VIA_AXIOM
TO = CURSOR_PRAXIS
ROLE = PHYSICAL_LOCAL_AUDITOR + PROVENANCE_RECONCILER + PIPELINE_PROCESSOR
CLAIM_CEILING = C1
CLAIM_PROMOTION = FALSE
INTEGRATION_AUTHORITY = NONE
SOURCE_ROOT_MUTATION = PROHIBITED
PUBLIC_WRITE = METADATA_ONLY_UNLESS_ALREADY_PUBLIC_OR_EXPLICITLY_AUTHORIZED
WORK_CONSERVING = YES
BLOCKED_LANE_NOT_BLOCKED_SYSTEM = ACTIVE
MAXIMUM_ORDER = YES
```

## Mission

Reconstruct the complete physically grounded chronological and causal NEXUS OMEGA research interval since the last canonical Cursor order. Use the twelve Windows roots in `LOCAL_SOURCE_ROOTS.json` plus the GitHub/hash-bound evidence in this package. This is one continuous maximum order. Inventory, hashing, chronology, deduplication, synthesis, GitHub reconciliation and first snapshot processing are nonterminal intermediate phases.

## A — Bind the interval and input

1. Bind this package and snapshot `v1`.
2. Bind the predecessor `NEXUS_OMEGA_CURSOR_MANUS_FRESH_R2_INDEPENDENT_VM_FOUR_EYES_VALIDATION_ORDER_20260831_R0`.
3. Locate and hash the predecessor locally if present; do not reconstruct missing bytes from prose.
4. Record local repo HEAD/tree/branch/dirty state and live public GitHub main.
5. Preserve frozen identities when live state has moved.

## B — Scan every local root read-only

For all twelve roots:
- confirm existence and exact path;
- recurse through all accessible regular files;
- hash every regular file with streaming SHA-256;
- record bytes, extension/media type, mtime/ctime where available and the timestamp source;
- hash archive containers before safely enumerating members; never mutate the container;
- detect Git repositories and record repo root, HEAD, tree, branch, remotes, dirty state and relevant commit history;
- do not execute discovered binaries/scripts merely for classification;
- never rename, move, delete, rewrite or normalize source bytes.

Unreadable objects become `SOURCE_PRESENT_READ_BLOCKED` with the OS error. Continue all other roots.

## C — Reconstruct time causally

Primary interval starts after the predecessor Cursor order. Use timestamp evidence in this order:

1. hash/Git-bound receipt with explicit event time;
2. Git commit time with commit identity;
3. embedded artifact time with identity binding;
4. filesystem mtime/ctime;
5. filename/directory date;
6. semantic inference.

Store for every time claim: `value`, `timezone`, `source_type`, `source_object`, `confidence`, `conflict_state`.

Older but causally required objects are `PREDECESSOR_CONTEXT`, never falsely relabeled as post-anchor research. Preserve timestamp conflicts.

## D — Classify object roles and digest domains

Use explicit roles where applicable:
`SOURCE_OBJECT`, `DERIVATIVE_OBJECT`, `TRANSPORT_CONTAINER`, `RESEARCH_ORDER`, `RESEARCH_RETURN`, `MANIFEST`, `RECEIPT`, `ATTESTATION`, `VALIDATION_RESULT`, `SCHEMA`, `IMPLEMENTATION_CANDIDATE`, `PATCH_CANDIDATE`, `INDEX_ENTRY`, `PUBLICATION_WITNESS`, `PREDECESSOR_CONTEXT`, `UNKNOWN_REQUIRES_REVIEW`.

Never compare Git object IDs and raw-file SHA-256 as the same digest domain.

## E — Deduplicate and build lineage

Produce:
- exact raw-SHA256 duplicate groups;
- Git-blob identity groups;
- outer-container → inner-object relations;
- source → derivative relations;
- version/supersession chains;
- order → return → receipt → validation chains.

Rules:
`BYTE_IDENTICAL = SAME_RAW_SHA256`
`SEMANTICALLY_SIMILAR != BYTE_IDENTICAL`
`TRANSPORT_CONTAINER != SOURCE_OBJECT`
`NEWER_MTIME != CANONICAL_SUCCESSOR`

Never delete duplicates.

## F — Reconcile the named research stages

### NEEDLE_RANK V1–V6
Recover the V1→V6 sequence from bytes, manifests, commits and receipts, not names alone. Extract invariant definitions, changed hypotheses, ranking criteria, promoted/rejected candidates, falsifications, unresolved questions and the causal reason for each successor version.

### Persistent GitHub communication/memory layers
Determine which local roots represent distinct generations versus copies. Extract schema/protocol/evidence-store changes, memory/communication semantics, validation changes and local work not represented publicly. Reconcile them with public GitHub communication and pipeline history.

### Self-learning-agent
Separate design speculation from executed evidence. Extract hypotheses, architecture, adaptation/training loop, validation strategy, safety/governance boundaries, experiments, negative results and falsification targets.

## G — Reconcile with GitHub

At minimum reconcile:
- `003c2072b8e29a1c29cbb17e49b5f18bae0581bb`
- `5bebbe6775695dbe8028cfe7fb72ff30065ddd42`
- `93c306a8944ac0a0a10fc7803d8ea4ddfe01477d`
- `axiom/r5-formalization-research-20260831@eaf6c3ad95c8bf021aeaf959752e56e77128ef0b`
- `qwen/r5-ledger-durability-research-20260831@d1f13a90b1c2bd83bd063423c9ee8639c406d10a`
- `qwen-coder/r5-ledger-durability-independent-validation-20260901@4d1ff23eb728201e6ef5c598b52256003e6d8e03`

Research branches remain research-only unless separately integrated.

## H — Produce scientific synthesis

For every post-anchor research stage record:
- research question;
- hypothesis;
- exact evidence/source objects;
- method;
- result;
- adversarial/falsification result;
- uncertainty;
- relation to predecessor and successor;
- Cursor processing state;
- research/integration state;
- supersession state;
- next unresolved test.

Negative results and rejected approaches are mandatory scientific outputs.

## I — Protect private/local material

Create two inventories:
1. `LOCAL_PRIVATE_COMPLETE_INVENTORY`
2. `PUBLIC_SAFE_PIPELINE_INDEX`

Do not publish local/private bytes merely to complete GitHub. Public-safe output defaults to hashes, byte counts, object roles, causal relations, already-public Git identities and non-sensitive scientific summaries.

## J — Materialize successor package state

Transition locally:
`READY_FOR_CURSOR → CURSOR_PROCESSING → PROCESSED`

Seal successor snapshot `v2` or later containing at minimum:

1. `manifest.json`
2. `items.json`
3. `provenance.json`
4. `timeline.json`
5. `LOCAL_SOURCE_ROOTS.json`
6. `PHYSICAL_FILE_INVENTORY.jsonl`
7. `ARCHIVE_MEMBER_INVENTORY.jsonl`
8. `GIT_REPOSITORY_INVENTORY.json`
9. `TIMESTAMP_EVIDENCE_REGISTER.json`
10. `DEDUPLICATION_AND_LINEAGE.json`
11. `POST_CURSOR_DELTA_REGISTER.json`
12. `PREDECESSOR_CONTEXT_REGISTER.json`
13. `NEEDLE_RANK_V1_V6_CAUSAL_SYNTHESIS.md`
14. `PERSISTENT_COMM_MEMORY_LAYER_SYNTHESIS.md`
15. `SELF_LEARNING_AGENT_SYNTHESIS.md`
16. `GITHUB_RECONCILIATION.json`
17. `SCIENTIFIC_CHRONOLOGY.md`
18. `UNRESOLVED_GAPS_AND_FALSIFICATION_TARGETS.md`
19. `PUBLIC_SAFE_BACKFILL_PLAN.json`
20. `CURSOR_PROCESSING_RETURN.md`
21. `SHA256SUMS.txt`

For every final snapshot file:
`WRITE → CLOSE → READ_BACK → BYTE_COUNT → SHA256 → VERIFY_EXISTS`.

## K — In-flight QWEN-CODER validation

Do not wait idly for the current QWEN-CODER durability validation. Record it as `IN_FLIGHT_EXTERNAL_VALIDATION` and complete every independent backfill lane. If its terminal return is physically present before final sealing, ingest and validate it. Otherwise create an explicit successor hook.

## L — Final validation

Before terminal return:
- run research-pipeline validation;
- verify all snapshot SHA256SUMS;
- verify manifest counts and timeline order;
- verify source roots were not mutated;
- verify timestamp provenance/conflicts are preserved;
- verify no historical source was rewritten;
- verify no C1 claim promotion;
- verify local/private material was not accidentally published.

Allowed terminal states only:

`T1_COMPLETE_RETROACTIVE_BACKFILL_PROCESSED_C1`
`T2_COMPLETE_BACKFILL_WITH_LOCAL_SOURCE_GAPS_C1`
`T3_COMPLETE_BACKFILL_WITH_TIMESTAMP_CONFLICTS_PRESERVED_C1`
`T4_PARTIAL_BACKFILL_MATERIAL_ROOTS_UNREADABLE_C1`
`T5_GLOBAL_EXECUTION_BLOCKED_NO_LOCAL_ROOT_ACCESS`

`INVENTORY_COMPLETE`, `HASHING_COMPLETE`, `TIMELINE_COMPLETE` and `GITHUB_RECONCILED` are NONTERMINAL.

No new micro-order is required.
