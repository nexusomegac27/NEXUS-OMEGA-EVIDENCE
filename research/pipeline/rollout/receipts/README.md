# Rollout receipts

Generated rollout-preparation receipts may be written here by:

```text
python scripts/artifact_rollout.py --root . process --write-repo
```

GitHub Actions emits the same package as a workflow artifact. Receipts are
process evidence only and do not close Authority-Gates.
