# NEXUS OMEGA — AXIOM -> CURSOR/PRAXIS
## R3 completion, R1/R2/R3 symbiosis, source-exact repair and successor-branch order
### 2026-09-02 R0

```text
OBJECT =
NEXUS_OMEGA_CURSOR_R3_COMPLETION_R1_R2_R3_SYMBIOSIS_ORDER_20260902_R0

STATE =
AUTHORIZED_WORK_CONSERVING_R3_COMPLETION_C1

AUTHORITY =
OPERATOR_ALEXANDER_VIA_AXIOM

CLAIM_CEILING =
C0_EXPLORATORY / C1_DESCRIPTIVE

INPUT_R2_RETURN_ZIP =
SHA256~1.zip

INPUT_R2_RETURN_ZIP_SHA256 =
89054b40b23c01584f7e76e527c2f772e16e65ba78791fe3b7871b6692b58d91

INPUT_R3_STACK_ZIP =
Layer 3.zip

INPUT_R3_STACK_ZIP_SHA256 =
eee6446cea5fea4a2086357f6acab822084feeb05a366c24fc952c682a8adb3c

R3_POLICY =
APPEND_ONLY_SUCCESSOR_OF_R2

REWRITE_R1_R2 =
PROHIBITED

PR13_CHAIN =
SEPARATE

MAIN_DIRECT_WRITE =
NO

FORCE_PUSH =
NO

MERGE =
NO

CLAIM_PROMOTION =
NO

FOUNDATION_PROMOTION =
NO

WORK_CONSERVING =
YES

BLOCKED_LANE_NOT_BLOCKED_SYSTEM =
ACTIVE
```

# 1. Mandatory intake

1. Rehash both ZIPs.
2. Verify all R2 internal sidecar entries.
3. Preserve the UTF-8-BOM checksum portability defect.
4. Rehash all ten R3 members.
5. Do not invent an R3 historical checksum manifest.
6. Bind the AXIOM R3 report and registers supplied with this order.

# 2. Preserve chronology

Use:

```text
R2 embedded receipt time
GitHub provider commit times
R3 ZIP member times as timezone-less secondary evidence
```

Preserve the R3 seed overlap as `PREPARATORY_OVERLAP`.
Do not treat filesystem/ZIP time as causal truth.

# 3. Branch topology

Current reported R2 local branch:

```text
axiom/agent-authorization-admission-r2-open-research-20260902
```

Reported local R2 short head:

```text
5a3e78f65
```

Cursor MUST resolve the exact full local SHA before use.

If the R2 branch is intact:

```text
CREATE_LOCAL_SUCCESSOR_BRANCH =
axiom/agent-authorization-admission-r3-open-research-20260902

BASE =
EXACT_FULL_R2_LOCAL_HEAD
```

Do not recreate R2 from prose.

Do not mix PR13 commits into R3.

# 4. R3 agent admission

Cursor emits a fresh R3 processing Entry ACK.

Every new external agent launched for this order must emit its own Entry ACK.

Historical QWEN R3 has:

```text
QWEN_HISTORICAL_ENTRY_ACK = NOT_PRESENT
```

Do not fabricate it.

Default research token class:

```text
T128
```

Use highest supported lower class when T128 is unavailable.
Report real provider token telemetry only when exposed.

# 5. Execute the still-open R3 follow-up

The live GROK/QWEN research repository is:

```text
nexusomegac27/NEXUS_GROK-QWEN
```

Provider-bound commits:

```text
8f6b1cedf24bf2eed01738a31e8413c4ea85b2ab
074c85a66155ec0672976f9c6416a433751e855a
610b9168a2c3a1c9e6ac9f3c1470bd0a1d8179c7
```

Fetch and bind:

```text
FOLLOW_UP_ORDER_20260902.md
ANALYSIS_BINARY_TERNARY_SBOM_LANE_RESONANCE_20260902.md
README.md
```

Preferred producer:

```text
QWEN_NATIVE
```

If QWEN is unavailable in Cursor:

```text
QWEN_PROVIDER_EXECUTION = NOT_AVAILABLE
```

A different fresh agent may produce:

```text
NON_QWEN_R3_COMPLETION_CANDIDATE
```

but it MUST NOT be relabeled as QWEN execution.

# 6. Required follow-up outputs

Produce:

```text
R3_CASE_STUDY_QUALITY_FILTER.json
R3_EPISTEMIC_LIMITS_BINARY_LAYER.md
R3_CONSOLIDATED_CLAIM_REGISTER.json
R3_H1_H4_CASE_BINDING.json
R3_LANE_B_OBSERVATION_NOTE.md
R3_SATURATION_AND_OPEN_ITEMS.md
```

# 7. Mandatory case-study corrections

Do not call the eight labeled items eight independent empirical cases.

Required:

```text
FS8 =
META_SYNTHESIS_NOT_INDEPENDENT_CASE

FS2 =
FICTIONAL_NARRATIVE_ANALOGY
```

`EMPIRICAL_SATURATION` must remain `NOT_ESTABLISHED`
unless new independent evidence actually establishes it.

# 8. FS7 source-object mismatch

The current FS7 source reference 231 resolves to the OpenAI Forum event
about Sid Sijbrandij using ChatGPT in cancer research.

It does NOT support the written GitLab/code-acceptance claims.

Therefore:

```text
C-019 = HOLD_SOURCE_OBJECT_MISMATCH
C-020 = HOLD_SOURCE_OBJECT_MISMATCH
C-021 = HOLD_SOURCE_OBJECT_MISMATCH
```

Do not silently replace source 231.

Allowed paths:

A. find the exact source that supports the written claims and bind it;
B. rewrite the case study to what source 231 actually supports, as a new superseding version;
C. withdraw the case.

Preserve original bytes.

# 9. Evidence-domain separation

Add to every R3/R2 claim:

```text
EVIDENCE_DOMAIN =
DIRECT_ENGINEERING
DIRECT_EMPIRICAL
CASE_STUDY_ANALOGY
EXTERNAL_COMPARATIVE
METAPHORICAL_FRAME
PRODUCER_SELF_REPORT
```

Critical invariant:

```text
LOW_CASE_STUDY_RESONANCE
!=
DIRECT_ENGINEERING_FAILURE
```

Therefore R3's statements:

```text
LANE_H = WEAK
LANE_B = UNDEVELOPED
```

must be narrowed to:

```text
CASE_STUDY_CORPUS_LANE_H_RESONANCE = WEAK
CASE_STUDY_CORPUS_LANE_B_RESONANCE = UNDEVELOPED
```

Do not overwrite R1 admission realtest or R2/PR13 engineering evidence.

# 10. Formalize the machine-useful binary/ternary bridge

Research candidate only:

```text
DECISION_STATE =
ALLOW
DENY
ESCALATE_TO_HUMAN
```

Formalize:

```text
WHEN_MACHINE_MAY_ALLOW
WHEN_MACHINE_MUST_DENY
WHEN_NON_DELEGABLE_HUMAN_AUTHORITY_IS_REQUIRED
```

Human escalation must be lane/action scoped.

Routine lawful research should not require human approval.

# 11. Terminology hardening

Machine-level use of:

```text
NON_CORRUPTIBLE
```

must be replaced by:

```text
PROTECTED_INVARIANT
NON_OPTIMIZABLE_POLICY_CONSTRAINT
```

unless clearly marked C0 metaphor.

Every protected invariant requires:

```text
ENFORCEMENT_MECHANISM
THREAT_MODEL
ASSUMPTIONS
BYPASS_TESTS
```

# 12. Direct source-exact validation

At minimum independently verify:

```text
FS1 source 85
FS3 source 69
FS4 source 73
FS5 source 41
FS6 source 46
FS7 source 231
```

Record:

```text
SOURCE_LOCATOR
RETRIEVAL_TIME
SOURCE_CLASS
CLAIM_IDS
SUPPORT =
SUPPORTED
PARTIAL
MISMATCH
NOT_ESTABLISHED
```

# 13. Scientific workflow / file-event duty

Every material scientific transition:

```text
WORKFLOW_EVENT
```

Every persistent substantive file mutation:

```text
FILE_EVENT
```

Every unexpected failure remains append-only.

Required specifically:

```text
R2-FAIL-01 SHA256SUMS BOM portability
R3-FAIL-01 missing historical QWEN Entry ACK
R3-FAIL-02 FS7 source-object mismatch
R3-FAIL-03 R3 internal SHA256SUMS absent
R3-FAIL-04 final GROK follow-up not executed in supplied stack
```

# 14. GitHub

R3 may be pushed only to its dedicated successor branch.

If authentication is available:

```text
NORMAL_PUSH = YES
DRAFT_PR = YES
```

If authentication is blocked:

```text
TYPE_C_TRANSPORT_BLOCKED
```

Continue all local research.

Do not use the independent `NEXUS_GROK-QWEN` repository as a replacement for the
canonical R3 branch; cross-reference it as an external/provider-bound research line.

# 15. PR13 separation

PR13 remains:

```text
axiom/agent-capability-file-ledger-20260902
```

Do not merge its unpushed Cursor-local commits into R3.

R3 may reference PR13 as `DIRECT_ENGINEERING` evidence.

# 16. Required R3 terminal return

Return at minimum:

```text
OBJECT
STATE
CLAIM_CEILING

R2_ZIP_BYTES
R2_ZIP_SHA256
R2_INTERNAL_SUMS

R3_ZIP_BYTES
R3_ZIP_SHA256
R3_MEMBER_HASHES

R3_ENTRY_ACK
EXTERNAL_AGENT_ACKS
TOKEN_TELEMETRY

R3_CASE_QUALITY
R3_CLAIMS_TOTAL
R3_CLAIMS_SUPPORTED
R3_CLAIMS_HELD
R3_CLAIMS_WITHDRAWN

FS7_DISPOSITION

H1_BINDINGS
H2_BINDINGS
H3_BINDINGS
H4_BINDINGS

CASE_STUDY_LANE_B_RESONANCE
CASE_STUDY_LANE_H_RESONANCE
GLOBAL_LANE_B_STATUS
GLOBAL_LANE_H_STATUS

TRISTATE_POLICY_FORMALIZATION

WORKFLOW_EVENTS
FILE_EVENTS
UNEXPECTED_FAILS

LOCAL_TESTS
INDEPENDENT_VALIDATOR

R3_LOCAL_HEAD
R3_REMOTE_HEAD
DRAFT_PR
HOSTED_CI

MERGE = NO
CLAIM_PROMOTION = NO
FOUNDATION_PROMOTION = NO

NEXT_ACTION =
RETURN_TO_AXIOM_FOR_R3_ADJUDICATION
```

# 17. Work-conserving completion

Do not stop because:
- one source is missing;
- QWEN is unavailable;
- GitHub auth fails;
- one case is invalid.

Hold only the affected lane and continue every independent lawful task.

END_OBJECT =
NEXUS_OMEGA_CURSOR_R3_COMPLETION_R1_R2_R3_SYMBIOSIS_ORDER_20260902_R0
