# R5 validation

R5 negative/adversarial fixtures, artifact bindings, reproducibility receipts, validation state, and bounded audit results belong here. Producer self-tests must remain distinguishable from independent validation.
