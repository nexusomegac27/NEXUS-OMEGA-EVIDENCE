# Validation domain

Persistent validation artifacts live under `validation/`: negative fixtures, artifact bindings, bounded machine status, and validation receipts.

Historical A83 files remain at their bound paths. New R5 validation corpora and bindings belong under `validation/r5/`.

`artifact-handoff-negative-fixtures-v1.jsonl` exercises fail-closed cases for
the GitHub artifact handoff protocol.
