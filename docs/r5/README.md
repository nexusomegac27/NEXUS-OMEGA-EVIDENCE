# R5 documentation lane

Stable R5 architecture, protocol specifications, design decisions, and migration documentation belong here once they are ready to leave active research.

Mirror R5 artifacts across `schema/r5/`, `scripts/r5/`, `tests/r5/`, `validation/r5/`, and `examples/r5/` where those lifecycle layers exist. Raw external-agent work remains under `research/r5/`.
