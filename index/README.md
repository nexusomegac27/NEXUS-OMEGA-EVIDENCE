# Scientific evidence index

`index/` is the discovery/index companion to the content-addressed scientific evidence store at `objects/`.

`index/v1/` is a stable compatibility surface. Do not mix communication-ledger records here; those belong under `communication/index/`.
