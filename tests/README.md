# Tests domain

Automated executable tests live under `tests/`.

Historical tests remain at stable paths. New R5 tests belong under `tests/r5/`. Fixtures whose primary role is persistent validation evidence belong under `validation/`, not `tests/`.
